
import helmet from 'helmet';

// Middleware to set a variety of secure HTTP headers
const securityHeaders = helmet({
    // Configure options as needed for the project
});

export default securityHeaders;
